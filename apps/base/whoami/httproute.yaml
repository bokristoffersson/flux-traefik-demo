apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: whoamiv1
spec:
  parentRefs:
    - name: traefik-gateway
  hostnames:
    - "fix.me"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: whoamiv1
          port: 80
